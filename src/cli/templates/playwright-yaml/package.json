{
  "name": "qa-agent-yaml-project",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate": "qa-agent generate",
    "test": "npm run generate && playwright test",
    "test:ui": "npm run generate && playwright test --ui",
    "test:headed": "npm run generate && playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "qa-agent": "github:pushpenderindia/QAAgent",
    "varlock": "latest"
  }
}
